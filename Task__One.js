const string = "Привет! Как дела?";

function getVovels(string) {
    for (i=0; i<=string.length; i++){
        var letter = string[i];
        if (letter=="и") {
            getLetter += letter;
        } else if (letter=="е") {
            getLetter += output;
        } else if (letter=="а"){
            getLetter += letter;
        }
    }
}

console.log(getLetter);